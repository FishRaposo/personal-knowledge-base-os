# Changelog

## 1.0.0 — Comprehensive local-first finalization

- Replaced the historical external/sibling compatibility dependency with the pinned internal `vendor_core` closure from `operator-shared-core` v1.3.0 (`dbf276a7708da65b55e1f10b35af634b300d1f07`); added provenance and MIT attribution.
- Preserved legacy routes and response keys while delivering vault namespaces, safe editing, dangling-link metadata, incremental indexing, explicit watchers, replayable SSE, saved searches, tag filters, deterministic flashcards, and local reviews.
- Documented optional infrastructure/provider boundaries and the default deterministic, broker-free, offline path.
- Added reproducible portfolio-evidence generation and verification. Exact gate outcomes, reproducibility hash, package validation, and manual visual/positioning handoff are recorded by the finalization receipt after clean-environment execution.

The prior initial implementation snapshot and its 100-test documentation claim are historical provenance, not a current verification statement.
