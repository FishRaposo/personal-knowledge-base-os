# Base URL of the FastAPI backend. When unset, defaults to http://localhost:8000.
# If the backend is unreachable, the UI automatically falls back to bundled demo
# data and shows a "Demo mode" indicator — so the app is fully explorable offline.
NEXT_PUBLIC_API_URL=http://localhost:8000
